Assuming a directory structure like...

  ./compiled-release
    ./{channel}
      ./{os_name}
        ./{os_version}
          ./v{version}.meta4
  ./release
    ./{channel}
      ./v{version}.meta4
